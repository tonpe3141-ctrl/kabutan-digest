# 日本株 日次振り返りシステム 設計書

作成日: 2026-09-17
対象: このリポジトリ（kabutan-digest）を土台に、毎日の日本株の市況振り返り・
セクター／銘柄の変化の分析・投資妙味のある銘柄の発掘までを一貫して行う仕組み。

この文書は次の順で進む。

1. 現状の診断（いま何があって、何が実際に動いていないか）
2. 設計 v1
3. v1 の採点（100点満点）と不足の洗い出し
4. 再設計 v2
5. 移行計画（何から手を付けるか）
6. 決めてもらう必要があること

---

## 1. 現状の診断

### 1.1 いまリポジトリにあるもの

| 仕組み | 実体 | 状態 |
|---|---|---|
| マーケットダッシュボード | `dashboard/` + `docs/`（GitHub Pages） | **動いているが、下記の欠陥あり** |
| ルールベースの見立て | `dashboard/commentary.py` | 動作中（if文の組み合わせで日本語を生成） |
| LLM分析（Claude Routine） | `dashboard/AI_ANALYSIS_TASK.md` + Routine 3本 | **停止中**（3本とも無効。9/8 の寄り前1回しか成功していない） |
| Routine の push を main へ自動マージ | `.github/workflows/merge-ai-branch.yml` | 動作するが、latest.json 1ファイルしか通さない |
| 株探ダイジェスト（旧） | `scraper.py` → Google ドキュメント | 自宅回線からのみ動く。Actions では株探に遮断される |

データ源は CNBC（相場）・Yahoo!ファイナンス（ランキング・市況記事）・
TDnet（適時開示）・日経公式（225構成銘柄）の4つ。すべて GitHub Actions のランナーから取れる。

### 1.2 実際に起きている問題（コミット履歴と Routine の記録から）

**問題A: GitHub Actions のスケジュールが毎日2〜7時間遅れている。**
直近7営業日の実行時刻（JST）:

| 区分 | 予定 | 実際の発火 | 遅れ |
|---|---|---|---|
| 寄り前 | 07:05 / 07:35 | 09:03〜09:52 | 約2時間。**寄り付き（09:00）に間に合っていない** |
| 前場 | 11:35 / 11:45 | 16:27〜17:06 | 約5時間。`adjust_slot` が大引に振り替えるため、**前場タブは一度も埋まっていない** |
| 大引 | 17:35 / 18:05 | 21:58〜00:51 | 4.5〜7時間 |

「予備を置く」「00分を避ける」という対策は効いていない。GitHub の cron は
ベストエフォートで、この遅れは構造的なもの。**cron に時計の役割を任せるのをやめる必要がある。**

**問題B: LLM分析の実行順序が逆だった。**
Routine「マーケット分析（寄り前）」は 08:05 JST に発火しているが、データを作る
Actions は 09:00 過ぎに動く。Routine は「データがまだ届いていない」と判断してスキップし、
結果として何も書かれなかった。前場・大引も同様。3本とも無効化されているのはこのため。

**問題C: 株探がクラウドから取れない。**
今回この環境からも実測した。株探・Yahoo!ファイナンス・TDnet・CNBC のすべてが
`EGRESS_BLOCKED`（Claude 側の送信プロキシで遮断）。GitHub Actions からは
Yahoo/TDnet/CNBC は取れるが株探は `405 Human Verification`。
**株探の記事は自宅回線からしか取れない**、というのが変わらない前提。

**問題D: 「発掘」の機能が無い。**
現状は「今日何が起きたか」を出すところまで。連続ランクイン（`streaks`）と
顔ぶれの入れ替わり（`ranking_delta`）はあるが、候補銘柄を溜めて追跡する仕組みが無く、
「注目した銘柄がその後どうなったか」を確かめられない。

**問題E: 使う側に届く経路が無い。**
更新されてもスマホに通知は来ない。自分で URL を開いて、更新されているか確かめる必要がある。

### 1.3 引き継ぐ設計の約束（CLAUDE.md より。これは v2 でも守る）

- 収集は必ず完走させ、取得失敗は「欠損」として描画する
- スロット（寄り前／前場／大引）は互いを上書きしない
- 分析値は「予測」と言わず、算出根拠を併記した相対的な並びとして出す
- ルールベースの文章は同じ数字から同じ文章が出るようにする（検証可能性）
- LLM が書く文章は `data` に無い数値を書かない

---

## 2. 設計 v1

v1 は「いまある部品を正しくつなぎ直す」設計。新しい部品は最小限にする。

### 2.1 目的

平日毎日、次の3つの時点で日本株を振り返り、スマホの固定 URL で読めるようにする。

| 時点 | 問い |
|---|---|
| 寄り前 07:30 | 昨夜の米国・為替・金利は、今日の日本株にどう効くか |
| 前場 12:00 | 前場は寄り前の想定どおりだったか。何が買われ、何が売られたか |
| 大引 16:30 | 1日の全体像。後場の変化。引け後の決算・修正。明日への持ち越し材料 |

### 2.2 構成

```
[時計]  Claude Routine（時刻どおりに発火する。実測 ±1分）
   │ 1. docs/data/trigger/{slot}.txt を書いた1コミットを自分のブランチに push
   ▼
[収集]  GitHub Actions（push イベントは数秒で起動する）
   │ 2. CNBC / Yahoo!ファイナンス / TDnet / 日経 を取得
   │ 3. analyze.py + commentary.py → docs/data/latest.json → main に push
   ▼
[分析]  同じ Routine が main の更新を待って続行
   │ 4. latest.json と記事本文を読み、LLM分析を ai_commentary に書く
   │ 5. claude/* ブランチに push → merge-ai-branch.yml が main へ自動マージ
   ▼
[表示]  GitHub Pages（docs/）をスマホで読む

[株探]  自宅の Mac で launchd が 12:10 / 17:40 に scraper.py を実行
        → docs/data/kabutan/YYYY-MM-DD.json を push（取れなければ欠損）
```

### 2.3 各部の設計

**時計（Routine）** 3本。寄り前 07:10、前場 11:40、大引 16:20 JST。
Routine の仕事は「合図を送る → 待つ → 分析を書く」の3つで、収集はしない
（この環境からは相手サイトに出られないため）。合図は `docs/data/trigger/{slot}.txt`
に発火時刻を書いた1コミットを push するだけ。GitHub の push イベントは cron と
違って即時に発火する。

注意: Routine の push 先ブランチは Routine ごとに固定されていて（例: `claude/upbeat-galileo`）、
Routine 側で名前を選べない。そのため**スロットの識別はブランチ名ではなくファイル名で行う**。

**収集（Actions）** `dashboard.yml` のトリガーを `schedule` から
`push: branches: ["claude/**"], paths: ["docs/data/trigger/*.txt"]` に変える。
push で変わった trigger ファイルの名前からスロットを決める。
cron は「保険」として1日1回だけ残す（Routine 側が落ちた日のため）。
`adjust_slot` の振り替えロジックは残すが、正常時には発動しなくなる。
`verify_ai_merge.py` は trigger ファイルも許可し、合図のコミット自体は main に
そのまま取り込まれる（中身は時刻1行なので害がない）。

**分析（Routine の後半）** `AI_ANALYSIS_TASK.md` の手順をほぼそのまま使う。
変更点は「STEP 1 の鮮度判定を、合図の時刻より新しいコミットが main に来るまで最大15分待つ」に
変えること。順序の問題（問題B）はこれで解消する。

**株探（自宅）** `scraper.py` の出力先を Google ドキュメントから JSON に切り替える
オプションを足し、launchd で1日2回動かす。取れた日だけ `docs/data/kabutan/` に
記事本文（昼刊・夕刊・市況・話題株）が入り、Routine の分析が読む素材に加わる。
Mac が寝ていて取れなかった日は、Yahoo!ファイナンスの市況記事だけで分析する。

**表示** 現状のタブ構成を維持。`ai_commentary` があればルールベースより上に出す
（既に実装済み）。

### 2.4 v1 で新たに作るもの

| 作業 | 規模 |
|---|---|
| `dashboard.yml` を push トリガー（`claude/**` の trigger ファイル）に変更 | 小 |
| `verify_ai_merge.py` の許可を `latest.json` ＋ `trigger/*.txt` に広げる | 小 |
| `AI_ANALYSIS_TASK.md` の待機ロジック | 小 |
| Routine 3本を作り直す（時刻変更・手順変更） | 小 |
| `scraper.py` に `--out json` を追加、launchd の plist | 中 |
| 分析タスクが株探 JSON も読むように手順追加 | 小 |

---

## 3. v1 の採点

100点満点。配点は「毎日使うために何が必要か」の重みで置いた。

| 観点 | 配点 | v1 | 理由 |
|---|---|---|---|
| **時刻どおりに届く** | 15 | 13 | Routine を時計にすれば解決する。Routine 自体が落ちた日の保険が cron 1本だけなのが減点 |
| **市場全体の動きが分かる** | 15 | 12 | 指数・ズレ・上げの中身・業種平均・ヒートマップまで既にある。33業種の公式騰落が無い |
| **注目セクターの変化が分かる** | 10 | 6 | 米→日の連想と225構成銘柄ベースの業種平均はある。「何日続いているか」「先週と比べてどうか」の時間軸が無い |
| **注目銘柄・テーマが分かる** | 10 | 5 | ランキング・連続ランクイン・開示はある。**テーマの単位で束ねる仕組みが無い**（半導体・電線・防衛…のような括りは人が頭でやっている） |
| **投資妙味のある銘柄の発掘** | 15 | 3 | **ほぼ無い。** 候補を溜めない、追跡しない、結果を検証しない |
| **分析の質（数字＋文脈）** | 10 | 7 | ルールベース＋LLM の二段構えは良い。株探が取れた日と取れない日で質が変わる |
| **情報源の広さ・強さ** | 10 | 6 | 株探は自宅 Mac が起きていないと入らない。Mac 依存は日次運用として脆い |
| **使う側に届く** | 5 | 1 | 通知が無い。URL を開きに行く必要がある |
| **壊れたときに気づける** | 5 | 2 | 疎通診断はあるが手動。「前場タブが2週間空だった」ことに誰も気づかなかった |
| **検証可能性・再現性** | 5 | 5 | 数字に無いことを書かない、根拠を併記する、テストで固定する、が守られている |
| **合計** | **100** | **60** | |

### 3.1 v1 に足りないもの（減点の大きい順）

1. **発掘の仕組みが無い（−12）。** ユーザーの目的の中で最も重い「投資妙味のある銘柄の発掘」に
   対して、v1 は日次の観察止まり。候補を溜め、理由を残し、その後の値動きで検証する
   「台帳」が必要。
2. **テーマの層が無い（−5）。** 上昇率上位に「フジクラ・古河電工・SWCC」が並んでいても、
   システムは「電線」というテーマを知らない。銘柄→テーマの辞書があって初めて
   「今日はどのテーマに資金が来たか」「そのテーマは何日目か」が言える。
3. **株探が Mac 依存（−4）。** 日次運用の要が「Mac が起きているか」に掛かる。
   代替経路か、Mac 側の堅牢化（スリープ解除・失敗通知）が要る。
4. **時間軸の分析が無い（−4）。** 「今日」しか見ていない。5日・20日で業種の強弱がどう
   移ったか、週次の振り返りが無い。
5. **届かない・気づけない（−7）。** 通知と監視。前場タブが空だった問題は監視があれば
   初日に分かった。
6. **公式の33業種騰落が無い（−3）。** 225構成銘柄の平均は代用品。TOPIX-17 か
   東証33業種を取れる公開ページの疎通確認が必要。

---

## 4. 再設計 v2

v1 の骨格（Routine＝時計、Actions＝収集、Routine＝分析、Pages＝表示）は残す。
v2 で加えるのは **「テーマ」「台帳」「時間軸」「届ける」「見張る」** の5つの層。

### 4.1 全体像

```
                    ┌──────────────────────────────────────────────┐
  自宅 Mac          │  株探コレクタ（launchd 12:10 / 17:40）          │
  （任意・あれば強い）│  昼刊・夕刊・市況・話題株・決算速報 → JSON push  │
                    └───────────────┬──────────────────────────────┘
                                    │ docs/data/kabutan/YYYY-MM-DD.json
                                    ▼
 ┌─────────┐  push合図  ┌─────────────────────────┐  main更新  ┌──────────────────┐
 │ Routine │──────────▶│ GitHub Actions（収集・分析）│──────────▶│ Routine（LLM分析）│
 │ 時計    │           │ CNBC / Yahoo / TDnet / 日経 │           │ 記事＋数字を読み  │
 │ 07:10   │           │ analyze → themes → ledger   │           │ 見立てと候補の理由│
 │ 11:40   │           │ → commentary → latest.json  │           │ を書く → push    │
 │ 16:20   │           └─────────────────────────┘           └────────┬─────────┘
 └─────────┘                                                          │ 自動マージ
                                                                      ▼
                       ┌──────────────────────────────────────────────────────┐
                       │ GitHub Pages: 寄り前 / 前場 / 大引 / 発掘ノート / 週報   │
                       └──────────────────────────────────────────────────────┘
                                                                      │
                       Routine が最後に PushNotification で見出し1行を送る
                       金曜 17:00 の Routine が週報（5日分の集計）を書く
                       毎朝の Routine が「昨日3スロット埋まったか」を点検し、欠けていれば通知する
```

### 4.2 データ源（v2）

| 区分 | 源 | 取れる場所 | 用途 |
|---|---|---|---|
| 相場（米・日指数、ETF、為替、金利、商品） | CNBC | Actions | 現状どおり |
| 日本株ランキング（売買代金・上昇・下落） | Yahoo!ファイナンス | Actions | 現状どおり |
| **年初来高値更新・出来高急増**（新規） | Yahoo!ファイナンス ランキング | Actions | 発掘の入口。**疎通とスラッグは要確認** |
| **業種別騰落**（新規） | Yahoo!ファイナンス or JPX | Actions | 33業種の公式値。**要確認**。取れなければ225ベースを継続 |
| 市況記事 | Yahoo!ファイナンス マーケットAIトピックス | Actions | 現状どおり |
| 適時開示 | TDnet | Actions | 現状どおり。v2 では「開示→翌日の値動き」を台帳で追う |
| 225構成銘柄 | 日経公式 | Actions | 現状どおり |
| **株探 昼刊・夕刊・市況・話題株・決算速報** | 株探 | **自宅 Mac のみ** | 文脈の主力。取れない日は Yahoo 記事で代替 |
| **銘柄→テーマ辞書**（新規） | 自前（`docs/data/themes.json`） | リポジトリ | LLM Routine が未知の銘柄に出会うたびに追記する |

株探は「あれば分析の質が上がるが、無くても日次は回る」位置づけに固定する。
Mac 依存を日次運用の必須条件にしない。これが v1 との違い。

### 4.3 新しい層の設計

#### (a) テーマ層 `dashboard/themes.py` + `docs/data/themes.json`

- 辞書: `{ "5803": {"name": "フジクラ", "themes": ["電線", "データセンター"]}, ... }`
- 日次の Actions が、ランキング上位・上昇率上位の各銘柄をテーマに束ね、
  「テーマ別の平均騰落・出現銘柄数・連続日数」を `theme_flow` として出す。
- 辞書に無い銘柄は `unknown_codes` として latest.json に残し、**LLM Routine が
  記事を読んだうえでテーマを付けて辞書に追記する**（LLM を「辞書の育成」に使う。
  日々の判定は辞書ベースで決定的に行うので再現性が保てる）。
- 初期辞書は株探のテーマ一覧を自宅からスクレイピングして種にする。取れなければ
  LLM に225構成銘柄＋直近60日のランキング出現銘柄を一括で付けさせる。

#### (b) 発掘台帳 `dashboard/ledger.py` + `docs/data/ledger.json`

「候補を溜める → 理由を残す → その後を測る」の3つを機械的にやる。

**入口（シグナル）** — どれか1つ以上で候補に載る。すべて既存データか Yahoo ランキングから作れる。

| シグナル | 判定 |
|---|---|
| 資金流入の継続 | 売買代金上位に3日以上連続（既存の `streaks`） |
| 新規の資金流入 | 売買代金上位に新規ランクイン ＋ 上昇率も上位 |
| 業績の変化 | TDnet で上方修正／増配 ＋ 当日または翌日に上昇率上位 |
| 決算後の続伸 | 決算短信の翌営業日以降も売買代金上位に残る |
| 高値更新 | 年初来高値更新ランキングに登場（Yahoo の疎通が取れた場合） |
| テーマの初動 | テーマ層で「連続日数1〜2日、出現銘柄数が急増」のテーマに属する |

**台帳の1行**

```json
{"code": "5803", "name": "フジクラ", "first_seen": "2026-09-10",
 "signals": ["資金流入の継続(4日)", "テーマ初動: 電線"],
 "price_at_flag": 8120, "themes": ["電線"],
 "track": {"d1": 1.8, "d5": 6.2, "d20": null},
 "status": "watching", "notes": "LLMが書く理由（記事の裏付けがあればURL）"}
```

**追跡** — 毎日の大引で `track` を更新（d1/d5/d20 のリターン、日経平均比の超過分）。
20営業日で自動的に `closed` にし、結果だけ残す。

**検証** — 週報で「今週フラグした候補の d5 中央値」「シグナル種別ごとの的中率」を出す。
これがあって初めて「このシグナルは効いている／いない」が言える。発掘機能は
**当てるためではなく、自分の見方を検証するため**に置く（CLAUDE.md の思想と同じ）。

#### (c) 時間軸 `dashboard/trend.py`

- 業種・テーマの 5日／20日の累積騰落と順位変化（既存 `history/` を使う。保持を60日→120日に）
- 「今日強い業種は、20日でも強いか（トレンド）、逆か（リバウンド）」を1行で出す
- 週報（金曜 17:00 の Routine）: 週の指数・業種・テーマの推移、台帳の成績、
  寄り前想定の的中状況（`verify_open` の累積）

#### (d) 届ける — Routine の最後に `PushNotification`

- 各スロットの LLM 分析が終わったら、見出し1行＋URL をスマホに push
- 台帳に新しい候補が載った日は、その銘柄名もひとこと添える

#### (e) 見張る — 朝の Routine に点検を足す

- 前日の3スロットが `latest.json` に揃っているか、`updated_at` が想定時刻から
  ±30分に収まっているか、各区画の `ok` が立っているか
- 欠けていれば通知。2日連続なら疎通診断ワークフローを `workflow_dispatch` で叩く
  （Routine から GitHub API を呼ぶには token が要る。合図の push と同じく
  `claude/probe` への push で代替できる）

### 4.4 スロットごとの中身（v2）

| 区分 | 時刻 | 中身 | 新規 |
|---|---|---|---|
| 寄り前 | 07:10 | 米指数・為替・金利・米セクター、想定オープン、日本セクター連想、前日引け後の開示、**昨日の台帳更新（何が候補入りしたか）**、LLM見立て | 台帳 |
| 前場 | 11:40 | 指数・ズレ・業種・ヒートマップ・ランキング・場中開示、寄り前想定の答え合わせ、**テーマ別の資金の向き**、（株探昼刊があれば）LLM見立て | テーマ |
| 大引 | 16:20 | 前場からの変化、引け後開示、**台帳の追跡更新と新規候補**、**テーマ別の連続日数**、（株探夕刊があれば）LLM見立て、明日への持ち越し | 台帳・テーマ |
| 週報 | 金 17:00 | 5日の業種・テーマ推移、台帳成績、想定オープンの的中率、来週の決算・イベント | 全部新規 |

### 4.5 LLM の役割の線引き

| やらせる | やらせない |
|---|---|
| 記事と数字を突き合わせて「なぜ」を書く | 数値の再取得・スクレイピング |
| 台帳の候補に「理由」を書く（記事の裏付けつき） | 台帳への候補の追加・削除（機械が決める） |
| 未知の銘柄にテーマを付けて辞書を育てる | 既知の銘柄のテーマの判定（辞書が決める） |
| 週報の総括を書く | 数字の集計（trend.py が決める） |

理由: LLM が決めた値は日によってぶれる。「入口の判定は機械、理由づけは LLM」に分けると、
週報で的中率を測ったときに「シグナルが悪かったのか、解釈が悪かったのか」を切り分けられる。

### 4.6 株探コレクタ（自宅 Mac）

- `scraper.py --mode morning|evening --out json` を足す。Google ドキュメント出力は残す。
- launchd で 12:10 / 17:40。`caffeinate` でスリープを抑止。
- 出力: `docs/data/kabutan/YYYY-MM-DD.json`（`{"morning": [...], "evening": [...]}`）
- push 先は main 直（ローカルの認証で可能）。push すると Actions は動かさない
  （`paths-ignore` で除外）。Routine の分析タスクは、あればこれを最優先の素材として読む。
- 取れなかった日はファイルが無いだけ。ダッシュボードは「株探記事なし」と出す。
- 将来 Mac 依存を外すなら: 自宅ネットワーク上の常時稼働機（Raspberry Pi 等）に
  同じ launchd 相当を置く。設計上はファイルの置き場が同じなので差し替えるだけ。

### 4.7 v2 の採点（自己採点）

| 観点 | 配点 | v1 | v2 | v2 の根拠 |
|---|---|---|---|---|
| 時刻どおりに届く | 15 | 13 | 14 | Routine 時計＋朝の点検で欠けに気づける。Routine 自体の障害は残る |
| 市場全体の動き | 15 | 12 | 13 | 33業種は疎通次第。取れなくても225ベース＋時間軸で改善 |
| 注目セクターの変化 | 10 | 6 | 9 | 5日／20日の時間軸、週報 |
| 注目銘柄・テーマ | 10 | 5 | 9 | テーマ層と辞書の育成 |
| 発掘 | 15 | 3 | 12 | 台帳（入口・追跡・検証）。シグナルの有効性は運用して初めて分かるので満点にはしない |
| 分析の質 | 10 | 7 | 8 | 株探がある日／ない日の差は残る |
| 情報源 | 10 | 6 | 7 | 株探を「あれば加点」に位置づけ直し、日次運用の必須から外した |
| 届く | 5 | 1 | 5 | PushNotification |
| 気づける | 5 | 2 | 4 | 朝の点検。自動修復までは踏み込まない |
| 検証可能性 | 5 | 5 | 5 | 「入口は機械、理由は LLM」の線引きで維持 |
| **合計** | **100** | **60** | **86** | |

残る14点は「実際に運用して分かること」に対応する: シグナルの有効性、Routine の
安定性、Yahoo の追加ランキングの疎通。設計段階でこれ以上を主張しない。

---

## 5. 移行計画

順番は「いま壊れているものを直す → 発掘の土台 → 育てる」。各段階が単独で価値を出す。

### 段階1: 時計を直す（最初にやる。効果が最も大きい）

1. `dashboard.yml` を「`claude/**` への push で `docs/data/trigger/*.txt` が変わったら」
   のトリガーに変更（cron は保険で1本残す）
2. `verify_ai_merge.py` の許可範囲を `latest.json` ＋ `trigger/*.txt` に広げる
   （段階3以降で `ledger.json` / `themes.json` も加える）
3. `AI_ANALYSIS_TASK.md` を「合図 → 待機 → 分析 → 通知」の手順に書き換える
4. Routine 3本を作り直す（07:10 / 11:40 / 16:20 JST）
5. 3営業日観察して、3スロットが時刻どおり埋まることを確認

完了条件: 前場タブが 12:00 に埋まっている日が3日続く。

### 段階2: 株探コレクタ

1. `scraper.py --out json`
2. launchd plist と `caffeinate`
3. 分析タスクが `docs/data/kabutan/` を読む

完了条件: 株探昼刊・夕刊が取れた日の LLM 見立てに、記事 URL が `sources` として付く。

### 段階3: テーマ層と台帳

1. `themes.json` の初期辞書（株探テーマ一覧 or LLM 一括付与）
2. `themes.py`（テーマ別集計）、`ledger.py`（入口・追跡）
3. Yahoo の追加ランキング（年初来高値・出来高急増・業種別）の疎通確認 → 取れたものだけ組み込む
4. ダッシュボードに「発掘ノート」タブ
5. LLM 分析タスクに「候補の理由づけ」「未知銘柄のテーマ付け」を追加

完了条件: 台帳に候補が載り、5日後に `d5` が埋まる。

### 段階4: 時間軸・週報・点検

1. `history/` の保持を120日に、`trend.py`
2. 金曜 17:00 の週報 Routine
3. 朝の Routine に点検と通知を追加

完了条件: 週報に「今週の候補の d5 中央値」「シグナル別の成績」が出る。

---

## 6. 決めてもらう必要があること

1. **自宅 Mac を株探コレクタとして使ってよいか。** 平日 12:10 と 17:40 に起動している
   （またはスリープ解除できる）前提が要る。難しければ段階2を飛ばし、株探なしで
   段階3へ進む。設計上は株探なしでも成立する。
2. **通知の経路。** Routine の PushNotification（Claude アプリ）で足りるか、
   メール（Gmail 連携）も要るか。
3. **台帳のシグナルの閾値。** 「売買代金上位3日連続」「d20 で自動クローズ」は初期値。
   運用しながら週報を見て調整する前提。
4. **旧の Google ドキュメント出力を残すか。** Claude デスクトップから要約させる用途が
   まだあるなら残す。無ければ `scraper.py` は JSON 出力専用に整理する。

---

## 付録: v1 と v2 の違いの要点

| | v1 | v2 |
|---|---|---|
| 時計 | Routine | Routine（同じ） |
| 収集 | Actions | Actions（Yahoo ランキング追加） |
| 株探 | 必須に近い扱い | あれば加点 |
| テーマ | 無し | 辞書＋LLM 育成 |
| 発掘 | 無し | 台帳（入口＝機械／理由＝LLM／検証＝週報） |
| 時間軸 | 当日のみ | 5日／20日、週報 |
| 通知 | 無し | Push |
| 監視 | 手動の疎通診断 | 朝の点検 Routine |

---

## 7. アプリ設計（v3: 出力の見せ方を作り直す）

v2 までは「何を集め、何を分析するか」の設計だった。ここでは **出力そのもの** を
スマートフォンで毎日読む前提で作り直す。実装は `docs/` に入れてあり、
このリポジトリの GitHub Pages がそのままアプリになる（ホーム画面に追加して使う）。

### 7.1 作り直す前の状態を、使い勝手・有効性・見やすさで採点する

実機相当（390×844）で描画して測った。

| 観点 | 配点 | 旧UI | 何が問題だったか |
|---|---|---|---|
| **要点が最初の1画面で分かる** | 15 | 6 | 日経の値と長文の見立てから始まり、「今日は何が起きたか」を3秒で掴めない |
| **読む量を選べる** | 15 | 3 | 大引タブは1本の縦スクロールで **約13画面分**。225銘柄のヒートマップ・30行の売買代金・30業種のバー・20件の開示がすべて展開されている |
| **目的の場所に行ける** | 10 | 2 | タブ以外のナビが無い。「開示だけ見たい」が13画面のスクロールになる |
| **発掘・追跡に使える** | 15 | 2 | 候補を溜める画面が無い。連続ランクインは売買代金の行のメタ情報に埋もれている |
| **過去と比べられる** | 10 | 0 | 昨日以前を見る手段が無い |
| **銘柄から引ける** | 10 | 3 | ウォッチリストはあるが、コードや社名でその日のデータを横断して引けない |
| **情報が正直（鮮度・欠損）** | 10 | 6 | タブに「07:00 / 12:00 / 17:30」と予定時刻を出しているが実際は2〜7時間遅れ。前場タブは「まだありません」のまま |
| **見やすさ（文字・色・密度）** | 10 | 8 | 配色（上昇=赤／下落=青）、ダークモード、数字の等幅は良い |
| **アプリらしさ（起動・オフライン・アイコン）** | 5 | 2 | manifest はあるが iOS 用の PNG アイコンが無く、オフラインでは何も出ない |
| **合計** | **100** | **32** | |

### 7.2 再設計の方針

1. **下端ナビで4画面に分ける。** 今日 / 発掘 / 履歴 / 銘柄。親指の届く位置に置く。
2. **「今日」は要点 → 見立て → 詳細の順。** 先頭に「3行サマリー」（見出し1行＋主要3指標＋
   物色のチップ）を置き、続けて見立て、その下に詳細カード。
3. **長い表は畳む。** 業種は上位5／下位5、ヒートマップは上昇12／下落12、売買代金は10位、
   値動きは8位、開示は8件を初期表示にし、「すべて見る」で残りを開く。
4. **セクションへ跳べる。** 上端に横スクロールのチップ列（要点・見立て・指数・業種・…）を置き、
   タップでその位置へ移動する。
5. **鮮度は実際の時刻で出す。** 時間帯ボタンには予定時刻ではなく **実際の更新時刻** を出し、
   取れていなければ「未取得」と書く。
6. **発掘画面を先に作る。** サーバ側の台帳（v2 の 4.3(b)）が整うまでは、端末側で
   `latest.json` と履歴から機械的に候補を組み立てる。判定は v2 と同じ考え方
   （連続ランクイン・新規流入・順位急上昇・上方修正／増配・決算で商い・修正後の資金流入）。
   `docs/data/ledger.json` が置かれたらそちらを優先して描画する。
7. **履歴画面。** `history/index.json` を新設し、各営業日の日経終値・騰落・売買代金上位3・
   想定オープンと実際・上方修正の件数を1行で並べる。タップで当日の上位10と上方修正を開く。
8. **銘柄画面。** ウォッチリスト＋横断検索。今日の指数採用銘柄・ランキング・開示・直近の履歴を
   コードまたは社名で引き、「売買代金20位」「9/16 売買代金上位」のように出所を添える。
9. **相場ニュースを画面に出す。** 収集していたのに描画していなかった記事本文を、
   折りたたみ一覧として各時間帯の末尾に置く。
10. **アプリらしさ。** PNG アイコン（180/512）、最小のサービスワーカー（ネットワーク優先、
    圏外時は最後に見た状態を出す）、`display: standalone`。

### 7.3 作り直したあとの採点

| 観点 | 配点 | 旧 | v3 | 根拠 |
|---|---|---|---|---|
| 要点が最初の1画面で分かる | 15 | 6 | 13 | 3行サマリーが先頭。見出し・日経・TOPIX・上昇銘柄数・強弱業種・開示件数が1画面に収まる |
| 読む量を選べる | 15 | 3 | 13 | 大引の初期表示は約13画面 → **約7.5画面**。畳んだ表は1タップで開く |
| 目的の場所に行ける | 10 | 2 | 9 | セクションチップ＋下端ナビ |
| 発掘・追跡に使える | 15 | 2 | 9 | 候補の抽出と理由は出る。d1/d5/d20 の追跡と成績はサーバ側の台帳待ち |
| 過去と比べられる | 10 | 0 | 8 | 履歴画面。業種・テーマの時間軸（v2 の trend.py）はまだ |
| 銘柄から引ける | 10 | 3 | 9 | 横断検索と出所表示 |
| 情報が正直 | 10 | 6 | 10 | 実際の更新時刻と「未取得」 |
| 見やすさ | 10 | 8 | 9 | 旧UIの良さを維持しつつ、カード見出しの折り返しなどを整理 |
| アプリらしさ | 5 | 2 | 5 | アイコン・SW・standalone |
| **合計** | **100** | **32** | **85** | |

### 7.4 残り15点は何か

- **追跡と成績（−6）**: 候補の d1/d5/d20 とシグナル別の成績は、サーバ側の台帳
  （v2 段階3）ができて初めて出る。画面側は `ledger.json` を読む準備を済ませてある。
- **時間軸（−2）**: 業種・テーマの5日／20日推移。履歴データに業種平均を残す変更が要る。
- **通知（−3）**: 更新が届いたことをスマホに知らせる経路（v2 段階4）。
- **前場が埋まらない（−2）**: 画面側は正直に「未取得」と出すが、根本は収集側の時計の問題
  （v2 段階1）。
- **株探の記事（−2）**: 自宅コレクタが動くまでは Yahoo の記事のみ。

### 7.5 画面と実装の対応

```
docs/index.html        4画面の枠、下端ナビ、時間帯ボタン、セクションチップ
docs/app.js            描画。renderPreopen / renderSession（今日）、renderDiscover（発掘）、
                       renderHistory（履歴）、renderStocks（銘柄）。foldable() が「畳む」の共通部品
docs/style.css         トークンとコンポーネント。上昇=赤／下落=青、ダークモード対応
docs/sw.js             サービスワーカー（ネットワーク優先・失敗時キャッシュ）
docs/icon-180.png      iOS ホーム画面用アイコン（512 は Android 用）
docs/data/history/index.json   履歴の一覧（store.write_history_index() が更新する）
docs/data/ledger.json  （未作成）サーバ側の台帳。あれば発掘画面が優先して読む
```

台帳の形は v2 の 4.3(b) のとおり。画面側は `entries[]` の各要素に
`code / name / first_seen / signals[] / themes[] / track{d1,d5,d20} / status / notes`、
`stats.by_signal[]` に `signal / count / d5_median` を期待する。

---

## 8. 構築記録（2026-09-17）

ユーザーの回答: 自宅 Mac がスリープしていても株探ニュースを使う／通知は Push のみ／Google ドキュメント出力は不要。
これを受けて v2 の段階1〜4を実装した。何を作り、何を実測で確かめ、何がまだ運用で確かめる段階かを残す。

### 8.1 実測で確かめたこと

| 確かめたこと | 結果 |
|---|---|
| 株探本体（kabutan.jp）に Actions から届くか | 405 Human Verification（変わらず） |
| **Yahoo!ファイナンスのニュース一覧に株探ニュースが配信されているか** | 届く。`/news/market` `/news/stocks` に「株探ニュース」として本文付きで配信。大引け記事（本日のポイント付き）、マーケット日報、**本日の【業種】騰落ランキング（東証33業種の率と上位3銘柄）**、PTS、増資・売り出し、投資部門別売買動向が読めた |
| **Google ニュース RSS（site:kabutan.jp）** | 届く。株探の全見出し（昼刊・夕刊・大引け・ストップ高安・レーティング日報・今朝の注目・明日の株式相場）と時刻。本文なし |
| Yahoo!ニュース媒体ページ / minkabu | 404 / 403。使わない |
| Yahoo ランキングの追加スラッグ | `yearToDateHigh`（年初来高値更新）と `volumeIncrease`（出来高急増）が 200。列構成が違うので `layout` で位置読み。`hot` は会員限定で無意味 |
| 東証33業種の公式ページ（JPX / Yahoo 業種別） | 取れない → 株探記事の本文から読む方式に |
| Claude Routine のセッションから外部サイトへ | すべて EGRESS_BLOCKED。収集は Actions に限定 |
| `create_trigger` で作った Routine（リポジトリ未接続）から push | **失敗**（ブランチが作られない）。Routine にはリポジトリのソースが要る |
| リポジトリを接続したセッション（`create_session` に source と outcome_branch）から push | **成功**。`git push origin HEAD:main` が `claude/routine-probe` に置き換えられて届いた |

### 8.2 Routine の配線（実測に合わせて確定）

```
[定時トリガー] ──persistent_session_id──▶ [スロット専用セッション（リポジトリ接続済み）]
                                              │ 手順書 STEP 0〜7 を毎回頭から実行
                                              │ push → claude/routine-{slot}（固定）
                                              ▼
                                   merge-ai-branch.yml が許可ファイルだけなら main へ自動マージ
```

- スロットごとに **リポジトリを接続したセッション**を1つ作り（outcome branch = `claude/routine-{slot}`）、
  定時トリガーはそのセッションを起こす。セッションは会話を続けるので、手順書は
  「毎回 STEP 0 から。前回の記憶を使わない」と明記した。
  **検証済み**: 定時トリガー（10:28 JST 19:28）で起こされた接続済みセッションからの push が
  `claude/routine-probe` に到達した。手動発火でも同様。
- 副作用: 接続済みセッションは push 後に下書き PR を自動で作る（9/8 の運用でも同じだった）。
  ブランチを消さない運用にしたので、スロットごとに 1 本の PR が開いたままになるだけで増えない。
  手順書に「PR の購読・定期チェックの予約はしない」と明記した。
- 代替（UI から作る場合）: claude.ai の Routine 作成画面でリポジトリ `tonpe3141-ctrl/kabutan-digest` を
  接続し、`dashboard/AI_ANALYSIS_TASK.md` の表にある時刻とプロンプトで 4 本作る。
  以前（9/8）にこの形で動いた実績がある。

### 8.3 作ったもの

| 層 | ファイル | 中身 |
|---|---|---|
| 時計 | `.github/workflows/dashboard.yml` | `claude/**` への `docs/data/trigger/*.txt` の push で即時起動。cron は保険 |
| 時計 | `tools/verify_ai_merge.py` | Routine が書いてよいファイル（latest / trigger / themes / ledger / weekly / index.html）を列挙 |
| 分析 | `dashboard/AI_ANALYSIS_TASK.md` | 合図 → 待機（最大20分）→ 見立て・台帳の理由づけ・辞書の育成 → push → PushNotification → 前日の点検。金曜は週報 |
| 株探 | `dashboard/sources/kabutan_news.py` | Google RSS の見出し（スロット別に選別）、Yahoo 配信の本文（優先順で最大12本）、33業種の解析 |
| ランキング | `dashboard/sources/yahoojp.py` `config.py` | 年初来高値更新・出来高急増（ETF/REIT 除外） |
| テーマ | `dashboard/themes.py` `docs/data/themes.json` | 279銘柄の初期辞書。テーマ別の出現数・平均騰落・連続日数・初動。未知銘柄は Routine が追記 |
| 台帳 | `dashboard/ledger.py` `docs/data/ledger.json` | 9種のシグナル（連続3〜6日、新規流入、順位急上昇≥8、上方修正・増配、決算で商い、上昇率×売買代金、年初来高値×商い、出来高急増×上昇、修正後の資金流入、テーマ初動）。スコア2以上で登録、1日12件まで。d1/d5/d20・日経比・20日で終了・シグナル別成績 |
| 時間軸 | `dashboard/trend.py` | 業種の5日/20日累積と「続伸／反発／押し目／続落」、日経とTOPIXの推移。履歴に業種・33業種・テーマ上位・breadth・大引時点の為替／原油を保存、保持120日 |
| アプリ | `docs/app.js` `style.css` | 33業種・テーマ・時間軸・株探・高値更新・出来高急増・週報のカード。要点チップに「候補入り」。履歴画面は指数の線（期間初日=0%）・業種ヒートマップ・列をそろえた日々の表。業種は毎日そろう日経225の業種平均に一本化（東証33業種は取れない日があり時点も混在） |
| 検証 | `tests/test_parsers.py` | 株探本文・33業種・見出し選別・別レイアウト・テーマ・台帳（入口/追跡/成績）・時間軸 |
| 撤去 | `scraper.py` `kabutan-digest.yml` | Google ドキュメント出力を削除。依存は requests / beautifulsoup4 のみ |

### 8.4 台帳の入口に入れた保険

- 売買代金上位に **7日以上連続**の銘柄（ソフトバンクG・東エレクなどの常連）は「資金流入の継続」に数えない。
  初回の試算で常連ばかりが候補になったため、3〜6日連続に限定した。
- 履歴が無い日は「テーマ初動」と言わない（前日の上位が分からないため）。
- 単独の弱いシグナル（スコア1）は台帳に載せない。

### 8.5 運用で初めて分かること（設計段階では断定しない）

- 手順書どおりに Routine が 20分以内にデータを受け取れるか（Actions の push 起動の遅延）。
- Yahoo 配信の株探記事の顔ぶれが日によってどう変わるか（前引けの業種記事が出るか）。
- シグナルの有効性（d5 の中央値、シグナル別の当たり）。週報で見て閾値を調整する。
- 辞書の育ち方（未知銘柄に Routine が付けるテーマの粒度）。

---

## 9. 相場温度計と逆張りガード（2026-09-23）

### 9.0 依頼と、解くべき問題

依頼の要点:

- マクロ（業績・モメンタム・原油・金利・為替など）を徹底的に点検し、**ざら場で使える**セクターと銘柄を提案する
- 「全部マイナス＝大底が近い（中長期の買い場）」「全部プラス＝天井が近い」という見方を仕組みにする
- 自分は **上がると買いたくなって高値を掴み、下がると損切りして、その後にリバウンドされる** を繰り返している。
  「良いニュースで売り、悪いニュースが続いたら出尽くしで買い」が相場では多いのでは（先週の AI 半導体で実感）
- 狼狽売り・高値掴みを **補助する機能** として設計・構築する

つまり欲しいのは「当てる予想」ではなく、**自分の衝動と逆の側に立つための、根拠つきの物差し**。
既存の発掘台帳は「売買代金上位に連続」「上昇率×売買代金」など **上がっているものを拾う（順張り）** 入口しかなく、
使い方次第ではむしろ高値掴みを後押しする。ここを埋める。

### 9.1 実測で確かめたこと

| 確かめたこと | 結果 |
|---|---|
| CNBC の日足 API（`ts-api.cnbc.com/harmony/app/bars/{sym}/1D/...`）に Actions から届くか | **届く。** 日経・TOPIX・S&P500・SOX・VIX・ドル円・米10年債・WTI、**東証個別株（7203.T）** まで1年超の日足（調整済み）が返る |
| 日足の本数 | 400日指定で 266本（日本の営業日）、3年指定で 735本。`charts/1Y.json` は 487本 |
| 国内金利・恐怖指数・先物 | **日本10年債 `JP10Y`・2年債 `JP2Y`・日経VI `.JNIV`・日経先物 `@NK.1` が取れる**（`.NKVI` `NIY.1` と業種指数 `.TOPX.ELEC` は空） |
| 業績の材料 | **日経公式の PER 表（`indexes.nikkei.co.jp/nkave/archives/data?list=per`）に届く**。列は「日付・加重平均・指数ベース」。当月分しか載らず、年月の指定は効かない → 毎日取って積み上げ、予想EPS＝日経平均÷指数ベースPER を作る |
| Yahoo!ファイナンスの時系列ページ | 200 は返るが埋め込み JSON が無い。使わない |
| 休場日の扱い | 9/21〜9/23 は休場だが、自前の履歴には前営業日の値が `stale` 付きで入っている。日足 API には休場日が無いので、指標は日足から計算する |
| TDnet の業績修正の表題 | 「業績予想の修正に関するお知らせ」とだけ書く会社が多く、**表題から上方／下方が分かるのは一部** |

### 9.2 設計 v1（最初に考えたもの）

1. **市場の温度計**: 6つの軸を −2〜+2 で採点し、合計を 0〜100 の「温度」にする。
   業績（TDnet の上方／下方修正の件数比）、モメンタム（日経の25日線乖離）、原油（WTI 20日変化）、
   金利（米10年 20日変化）、為替（ドル円 20日変化）、センチメント（VIX 水準）。
   温度 80 以上を「天井圏の典型」、20 以下を「大底圏の典型」と表示する。
2. **セクター**: 日経225の業種平均の 5日／20日（既存の `trend.py`）で「押し目」「過熱」を付ける。
3. **銘柄**: 225採用銘柄の RSI と25日線乖離で「売られすぎ」「買われすぎ」を並べる。
4. 画面は「今日」タブに温度計のカードを1枚足す。

### 9.3 v1 の採点（100点満点）

配点は「依頼者が衝動と逆の側に立てるか」に効く重さで置いた。

| 観点 | 配点 | v1 | 減点の理由 |
|---|---|---|---|
| **マクロの網羅性**（業績・モメンタム・過熱・原油・金利・為替・米株・センチメント） | 15 | 9 | 米国株の地合い、需給（騰落レシオ）、ニュースの論調が無い。業績は TDnet 表題だけで、方向が分かる件数が少ない |
| **逆張りの判定ロジック** | 15 | 8 | 水準しか見ていない。「悪いけれど改善し始めた」（最良の買い場）と「悪化の途中」を区別できない。全部悪い／全部良いの**一致度**を数えていない |
| **その主張は本当か（検証）** | 10 | 0 | 「全部良い時が天井」が**この相場で成り立つのか**を一度も確かめていない。強気相場ではむしろ外れる可能性がある |
| **ざら場で使えるセクター提案** | 10 | 5 | 業種平均の騰落だけ。マクロとの相性（金利上昇で銀行、円安で輸出など）と業績が入っていない。時間帯ごとの使い方が無い |
| **ざら場で使える銘柄提案** | 10 | 4 | 指標の並びだけで、**どこまで待てばよいか（価格の目安）** が無い。上昇トレンド中の押し目と、下落トレンドの途中を区別しない |
| **ニュースの出尽くし判定** | 10 | 0 | 無い。「好材料で売られる」「悪材料で下げ止まる」を検出する仕組みが無い |
| **行動バイアスへの介入**（高値掴み・狼狽売りを、その場で止める） | 15 | 3 | 相場全体の温度を眺めるだけ。**買いたい／売りたい瞬間に**自分の銘柄で確かめる手段が無い。自分の判断を振り返る仕組みも無い |
| **提案の追跡と成績** | 5 | 0 | 提案がその後どうなったかを測らない（台帳の順張りシグナルとの比較もできない） |
| **データの信頼性・欠損時の頑健性** | 5 | 3 | 自前の履歴は14営業日しか無く、休場日の重複も混ざる。25日線も RSI も数週間は計算できない |
| **届く・約束の順守**（通知、予測と言わない、入口は機械） | 5 | 4 | 温度が極端になっても通知されない |
| **合計** | **100** | **31** | |

### 9.4 不足をどう解決したか（v2）

| 不足 | 解決 |
|---|---|
| 履歴が短い・休場日が混ざる | **CNBC の日足を取得してキャッシュ**（`docs/data/cache/bars.json`）。マクロ10系列は3年、個別株は225採用＋ウォッチリスト＋台帳＋テーマ辞書＋直近の業績修正銘柄を130営業日。毎日末尾だけ取り直し、株式分割などで過去値がずれたら全期間を取り直す |
| 軸が足りない | **8軸**に拡張: 業績（**日経平均の予想EPSの20日変化**＋TDnet 修正の方向）、モメンタム（日経 60日騰落と75日線）、過熱感（RSI14・25日線乖離・**225採用銘柄から自前で計算する騰落レシオ**）、原油、金利（米10年・**日本10年**）、為替、米国株（S&P500・SOX 20日）、センチメント（VIX・**日経VI**・**見出しの論調**） |
| 水準しか見ていない | 各軸に **「10営業日前と比べて改善／悪化」** を付ける。温度が低く改善し始めた軸が2つ以上 →「底打ちの兆し」、温度が高く悪化し始めた軸が2つ以上 →「天井打ちの兆し」 |
| 一致度 | 「追い風 k／向かい風 m／中立」の本数を出し、**全軸が同じ向き**を独立に判定（全部マイナス／全部プラス） |
| 検証が無い | **日足で温度計そのものを過去に当てる（バックテスト）**。価格から計算できる軸だけで毎日の温度を再現し、温度帯ごとの「20日後・60日後の日経の騰落（中央値・上昇した割合・日数）」を出す。依頼者の仮説が**この相場で成り立つかどうかを数字で見せる**（成り立たないならそう出る） |
| セクター提案が弱い | 225採用銘柄の日足から **業種ごとの等ウェイト指数** を作り、5/20/60日・25日線乖離・RSI・過熱度（5日騰落 ÷ その業種の普段の振れ幅）を計算。**マクロ感応度表**（業種 × 金利・円安・原油・米半導体・米株）で「この20日のマクロがこの業種に追い風か」を足し、業績修正の方向も足す。分類は「押し目（買い場候補）」「売られすぎ・下げ止まり」「上昇トレンド」「過熱（追いかけ注意）」「下落トレンド（様子見）」 |
| 銘柄提案に水準が無い | 各銘柄に **25日線の価格** と「現値から何％下か」を付ける。分類は「押し目買い候補（60日上昇・25日線まで調整・RSI 30〜50）」「売られすぎ反発候補（RSI≦30 か25日線−10%以下、かつ当日下げ止まり）」「高値掴み注意（RSI≧75・25日線+15%・5日+15% のどれか）」 |
| 出尽くし判定が無い | **開示への値動きの反応**で機械的に判定: 上方修正・増配のあと株価が下がった →「好材料出尽くし」、下方修正・減配のあと下げ止まった／上がった →「悪材料出尽くし（アク抜け）」。**テーマ単位**でも、見出しの論調が好材料一色（直近3日）で過熱 →「好材料出尽くしに注意」、悪材料の見出しが続くのに下げ止まった →「悪材料出尽くしの兆し」（先週の AI 半導体の型） |
| その場で止める手段が無い | **「買う前／売る前チェック」**（温度計タブ）。コードを入れて「買いたい／売りたい」を押すと、その銘柄の短期の上げ幅・RSI・25日線・出尽くし・業種・相場温度から、依頼者の癖（上がると買う／下がると売る）に当てはまるかを判定し、**待つ価格の目安と分割の案**を出す。判断をこの端末にメモし、あとで「その後どうなったか」を表示する |
| 提案を測らない | 大引で当日の提案（業種・銘柄・注意）を `docs/data/thermo_track.json` に記録し、5日後・20日後の騰落（日経比）を追う。種類別の成績（押し目／売られすぎ／悪材料出尽くし／高値掴み注意）を出し、**台帳の順張りシグナルと並べて**比べられるようにする |
| 届かない | Routine の通知に温度帯を添え、「過熱」「総悲観」に入った日と帯が変わった日は見出しの先頭に出す。LLM は温度計の数字を変えず、記事で背景を補う（入口は機械、理由は LLM） |

### 9.5 v2 の採点

| 観点 | 配点 | v1 | v2 | 根拠 |
|---|---|---|---|---|
| マクロの網羅性 | 15 | 9 | 13 | 8軸（国内金利・日経VI・予想EPS まで）。投資主体別売買・信用残は取れる公開源が無く、入っていない |
| 逆張りの判定ロジック | 15 | 8 | 14 | 水準＋変化の向き＋一致度。帯の境界は固定値で、相場の局面に合わせた調整は運用で見直す |
| その主張は本当か（検証） | 10 | 0 | 8 | 3年の日足でバックテスト。業績とニュースの軸は過去データが無く検証の外、20日後の重なり（同じ期間を何度も数える）で標本は見た目より少ない |
| ざら場のセクター提案 | 10 | 5 | 9 | 業種指数・過熱度・マクロ感応度・業績。前場の時点では当日の騰落を場中値で差し込む |
| ざら場の銘柄提案 | 10 | 4 | 9 | 押し目・売られすぎ・注意の3種と、25日線の価格 |
| ニュースの出尽くし判定 | 10 | 0 | 8 | 開示への反応（銘柄）と見出しの論調（テーマ）。表題に方向が無い修正は拾えず、論調はキーワード辞書で粗い |
| 行動バイアスへの介入 | 15 | 3 | 14 | 買う前／売る前チェック、待つ価格、判断メモの振り返り。実際に押してもらえるかは運用次第 |
| 提案の追跡と成績 | 5 | 0 | 5 | 種類別の 5日／20日成績、順張りの台帳と並べて比較 |
| データの信頼性 | 5 | 3 | 5 | 日足キャッシュ。取れなければ温度計は「材料不足」の軸を除いて計算し、何軸で出したかを表示 |
| 届く・約束の順守 | 5 | 4 | 5 | 温度帯の通知。「予測」と言わず、根拠と過去の成績を併記 |
| **合計** | **100** | **31** | **90** | |

残る10点は「運用して初めて分かること」: 帯の境界値の妥当性、業績とニュースの軸の有効性（過去データが無い）、
チェック機能を実際に使う習慣がつくか。設計段階ではこれ以上を主張しない。

### 9.6 構成

```
dashboard/sources/cnbc.py   fetch_bars()   日足（調整済み）の取得
dashboard/bars.py           日足キャッシュ（docs/data/cache/bars.json）の差分更新と分割の検出
dashboard/thermo.py         温度計の本体（すべて純関数。テストで固定）
                              indicators … SMA / RSI / 乖離 / 過熱度
                              market_thermo … 8軸・温度・帯・一致度・変化の向き
                              backtest … 温度帯ごとの 20日／60日後
                              sector_board … 業種指数・分類・マクロ感応度・おすすめ順
                              stock_board … 押し目／売られすぎ／高値掴み注意、25日線の価格
                              exhaustion … 開示への反応とテーマの論調で出尽くしを判定
                              news_tone … 見出しのキーワード論調
                              track … 提案の記録と 5日／20日の成績
dashboard/config.py         MACRO_BAR_SYMBOLS / SECTOR_MACRO_SENS / 温度計の閾値
docs/data/thermo.json       アプリが読む温度計（市場・業種・銘柄の指標・バックテスト・成績）
docs/data/thermo_track.json 提案の記録（大引で追記）
docs/app.js                 「温度計」タブ（作戦）と、今日タブの要点カード、買う前／売る前チェック
```

時間帯ごとの使い方:

| 時間帯 | 温度計が出すもの |
|---|---|
| 寄り前 07:10 | 前日までの温度と帯、米国の夜間を足した変化。**寄り付きの成行を避けるべき銘柄**（高値掴み注意）と、寄りで拾う候補（押し目） |
| 前場 11:40 | 場中値を差し込んだ業種・銘柄の分類。**後場の作戦**: 押し目に来た業種、前場で過熱した銘柄、地合いで売られた保有銘柄（狼狽売り注意） |
| 大引 16:45 | 日足を更新して温度・業種・銘柄を確定、提案を記録、翌日への作戦 |

### 9.7 運用で確かめること

- 初回の大引で個別株 約400銘柄の日足を取る（ホストあたり 0.2 秒間隔で 2〜3 分）。以後は末尾だけ。
- 予想EPSの軸は PER を積み上げてから（最低10営業日）点く。それまで業績の軸は TDnet の修正だけで採点する。
- 見出しの論調は株探の見出し（Google RSS）と市況記事の語で数えるだけの粗い物差し。帯の境目（強気 62%／75%）は運用で見直す。
- 帯の境目（80／62／38／20）はバックテストの結果を見て調整する。調整したら tests の期待値も直す。
- 判定の成績（`thermo_track.json`）は 5日後が出るまで1週間、20日後は1か月かかる。週報で順張りの台帳と比べる。

### 9.8 実データでの初回の結果（2026-09-23、Actions で試運転）

所要77秒（個別株 362/366 銘柄の日足を初回取得）。`bars.json` 399KB、`thermo.json` 122KB。

| 帯 | 日数／局面 | 20日後の日経（中央値・上昇割合） | 60日後 |
|---|---|---|---|
| 過熱 | 1日／1回 | −0.1%・0% | +6.3%・100% |
| 楽観 | 200日／36回 | +3.0%・66% | +3.6%・62% |
| 中立 | 378日／47回 | +1.4%・62% | +4.5%・71% |
| **悲観** | 74日／14回 | **+7.6%・84%** | **+15.9%・94%** |
| 全期間 | 655日 | +2.0%・65% | +5.2%・70% |

2024-01〜2026-09 の強い上昇相場では、**温度が低い（悪材料がそろった）日のほうが、その後の上昇がはっきり大きかった**。
依頼者の見立て（全部マイナスは買い場）と整合する。一方で「過熱」は1日しか無く、「全部プラスは天井」は
この期間では検証できるだけの標本が無い（楽観の日の60日後は全期間平均を下回る程度）。画面にはこの表をそのまま出す。

同じ試運転で見つけた問題: 下方修正のあと +50% 上がった銘柄が「悪材料出尽くし」に入っていた（TOB など別の材料の可能性）。
開示から ±12% 以上動いたものは「開示後に急変（別の材料の可能性）」として出尽くしの判定から外した。

---

## 10. 発掘と作戦を「勝てる」道具にする（2026-09-25）

### 10.0 依頼と、解くべき問題

依頼: プロのアナリストの目で、発掘と作戦のタブを「投資で勝てるように使える」ものにする。

勝ち負けを決めるのは「どの銘柄を見つけたか」より、**①その型がいまの相場で効いているか ②どこで入り、
どこで損切り、どこまで狙うか ③1回の損をどれだけに抑えるか** の3つ。9章までの仕組みは「候補を見つける」
「衝動を止める」まではできていたが、この3つが無かった。

### 10.1 実データで見つけた問題

| 問題 | 実測 |
|---|---|
| **台帳が休場日を営業日として数えていた** | 9/21〜23（休場）にも大引の処理が走り、9/18 に載った3銘柄の d1 が前営業日と同じ価格で 0.0% と記録された。9/17 の銘柄の「d5」（中央値 −5.6%）は実際には **2営業日後** の値だった |
| 開示だけで載った銘柄が追跡されていない | ランキングに価格が無いため 12件中4件が `price_at_flag = null` のまま、d1 も d5 も付かない |
| 成績が生の騰落だけ | 相場全体が下げた週は、どの入口も「負け」に見える。銘柄選びの効き目と区別できない |
| 型を固定で「拾う候補」と出していた | 371銘柄×約50営業日で型ごとに検証すると、**「売られすぎ・下げ止まり」は20日後に全銘柄を −2.5% 下回っていた**（10.4）。作戦タブはそれを逆張りの候補として出していた |
| 計画が無い | 押し目の「待つ価格（25日線）」はあるが、損切り・利確・R倍・株数が無い |

### 10.2 採点（100点満点）

| 観点 | 配点 | 前 | 後 | 根拠 |
|---|---|---|---|---|
| 型が効いているかの検証 | 15 | 0 | 12 | 先読みしない銘柄レベルの検証を毎日。期間は約50営業日（1つの局面）で、終値だけ |
| 売買計画（入る・損切り・利確・R倍） | 15 | 3 | 13 | 機械的な計画と、同じ計画で売買した場合の平均R・勝率 |
| 資金管理（株数） | 10 | 0 | 9 | 資金×1回の損の上限÷損切り幅、100株単位、1銘柄の上限 |
| 発掘→作戦のつながり | 15 | 3 | 12 | 台帳の銘柄を「いまの位置」で分け、押し目圏だけ計画を出す。台帳の銘柄は作戦ボードで先に並ぶ |
| 測定の正しさ | 15 | 6 | 14 | 東証の営業日・日足（分割調整済み）の終値・日経比・全銘柄比 |
| 候補の質 | 10 | 5 | 8 | 相対力（60日騰落の順位）、R倍1.5未満・損切り12%超・過熱を外す。売買代金などの流動性は見ていない |
| 見送る判断を出せる | 10 | 2 | 9 | 効く買いの型が無い日は「見送る・小さく」を先頭に出す |
| 正直さ | 5 | 4 | 5 | 「計画上のR倍」と「実績の平均R」を必ず並べる。件数不足は判定しない |
| 届く | 5 | 3 | 5 | 今日タブの要点・ルールベースの見立て・Routine の分析に型の判定と作戦ボードを渡す |
| **合計** | **100** | **26** | **87** | |

### 10.3 作ったもの

```
dashboard/thermo.py     trade_plan      買う目安（押し目は25日線まで待つ、ほかは現値）・損切り（20日安値の1%下、
                                        日々の値幅の1.5〜3倍に収める）・利確の目安（押し目・リーダーは60日高値、
                                        売られすぎ・出尽くしは25日線）・R倍
                        simulate_plan   計画どおりに売買したら、を終値で再現（指値は5日以内に約定しなければ見送り、
                                        最長20営業日）
                        rs_ranks / is_leader  相対力（60日騰落の順位）と「相対力リーダー」の型
                        setup_backtest  型ごとに、型に入った日だけを1件として 5日・20日後の全銘柄比、上回った割合、
                                        計画の平均R・勝率、直近20営業日の分。判定は「効いている／効いていない／はっきりしない」
                        action_board    型に当てはまり R倍≥1.5・損切り12%以内・過熱でない銘柄を、型の成績→台帳→R倍の順に
dashboard/thermo_run.py 上をつなぎ thermo.json に setups / board / lists.leaders / 各銘柄の plan・rs を載せる。
                        大引で台帳を日足で測り直す。休場日は判定の記録も台帳も進めない
dashboard/ledger.py     retrack（東証の営業日で d1/d5/d20 と日経比を測り直す）、休場日の防御、開示だけの銘柄の価格の取り足し、
                        成績に日経比・日経に勝った割合・20日後・件数不足の印
docs/app.js             作戦: 作戦ボード（株数つき）・いま効いている型・一覧に計画の行・買う前チェックに計画と型の成績
                        発掘: 見つけた銘柄はいまどこにいるか（押し目圏／追いかけ注意／崩れた／様子見）・入口ごとの成績表
```

株数の計算に使う資金はこの端末（localStorage）にだけ保存し、どこにも送らない。

### 10.4 実データでの初回の結果（2026-07-10〜09-24 に点灯、372銘柄）

| 型 | 点灯 | 5日後 全銘柄比 | 20日後 全銘柄比 | 上回った割合 | 計画どおりの平均R・勝率 | 判定 |
|---|---|---|---|---|---|---|
| 押し目 | 687 | +0.3% | 0.0% | 49% | +0.16R・44% | はっきりしない |
| 売られすぎ・下げ止まり | 264 | −0.7% | −2.5% | 42% | +0.12R・41% | **効いていない** |
| 相対力リーダー | 453 | −1.0% | −2.2% | 37% | −0.15R・40% | **効いていない** |
| 高値掴み注意（警告） | 363 | −1.8% | −4.3% | 39% | — | 警告どおり |
| 下落トレンド（警告） | 551 | +0.3% | +0.8% | 53% | — | 警告が外れている |

この期間は「上がった株が売られ、下げていた株が戻す」局面で、上がっている銘柄を追う型（リーダー・台帳の順張りの入口）は
負けている。一方、逆張りの「売られすぎ・下げ止まり」も効いていない（当日プラスで引けた、という1日の反発は続かない）。
効く型は局面で入れ替わるので、固定の意見ではなく毎日測って出す。この日は **全銘柄を上回る買いの型が無い** ため、
作戦ボードの先頭は「見送る・小さく試す」になる。

### 10.5 限界と、運用で確かめること

- 終値だけで再現している（場中の高安・寄り付きのギャップ・売買コストは含まない）。損切りは実際には場中に掛かるので、
  実績は再現より少し悪くなる。
- ユニバースは225採用＋テーマ辞書＋台帳＋ウォッチリスト（約370銘柄）。中小型の多くは入っていない。
- 検証期間は日足の保持（130営業日）から約50営業日。1つの局面の結果で、長期の優位性は主張しない。
- 型と計画の閾値（押し目の RSI 30〜50、損切りの値幅 1.5〜3倍、R倍 1.5、判定の ±0.5%）は運用で見直す。直したら tests も直す。
- 台帳の成績は、休場日の修正後の測り直しで 9/17 の分から正しくなる。最初の d5 は 9/29（9/17 に載った分）。


---

## 11. 「今日どうするか」を先頭に出し、負けにくい計画にする（2026-09-26）

### 11.0 依頼と、解くべき問題

依頼: 勝率を上げたい。取得する情報の品質と、表示するコンテンツを全体を見渡して見直す。

10章までで「型の効き目の検証」「売買計画」「株数」はそろったが、実データと画面を通して見ると次の問題が残っていた。

| 問題 | 実測・根拠 |
|---|---|
| **損切りが近すぎる** | 損切りの下限が日々の値幅の1.5倍で、1日の揺れの範囲に入っていた。押し目の計画を再現すると 444回中 207回（47%）が損切り。依頼者の「損切りした後にリバウンドされる」を計画自体が再現していた |
| **R倍が大きく見えすぎる** | 利確の目安（60日高値）が20営業日では届かない距離にあることが多く、ボードの上位は「計画上のR倍 5.3、実績の平均 +0.27R」のような並びだった。R倍の順に並べると、届かない利確の銘柄ほど上に来る |
| **効いていない型の銘柄がボードに載る** | 「売られすぎ・下げ止まり」「相対力リーダー」が全銘柄を下回っていても、作戦ボードには載っていた（注意書きだけ） |
| **結論がどこにも無い** | 温度・型の成績・ボード・ウォッチの見張りが別々のカードにあり、「今日は買うのか・どれくらいか」を自分で組み立てる必要があった |
| **遅れて発火した保険の cron が、良いデータを悪いデータで上書きする** | 9/25 大引: 16:55 の実行では東証33業種と決算速報の記事が取れていたが、18:05 の保険 cron が 23:36 に発火して取り直し、業種騰落ランキングの記事が一覧から落ちて東証33業種が消えた |
| **寄り前の想定オープンの基準が場中値になる** | 07:35 の保険 cron が 09:46 に発火した日（9/25）、CNBC の `.N225` の `last` は当日の場中値。これを前日終値として想定オープンを計算していた |

### 11.1 実データでの検証（389銘柄 × 2026-07-13〜09-25、`setup_backtest` と同じ方法）

**損切りの幅**（日々の値幅＝60日の日次騰落の標準偏差。終値で判定）

| 損切りの範囲 | 押し目 平均R・勝率 | 売られすぎ 平均R・勝率 | リーダー 平均R・勝率 |
|---|---|---|---|
| 1.5〜3倍（従来） | +0.27R・45% | +0.17R・42% | −0.11R・40% |
| 2〜3倍 | +0.27R・49% | +0.08R・44% | −0.11R・41% |
| **2.5〜3.5倍（採用）** | **+0.28R・53%** | +0.11R・46% | −0.06R・42% |
| 3〜4倍 | +0.24R・56% | +0.16R・50% | −0.03R・44% |

平均R（1回の損を1とした損益）はほぼ変わらず、勝率だけが上がる。R で測っているので、損切りを広げたぶん株数は
自動で減り、1回の損の金額は同じ。2.5倍は「ふだんの1日の揺れ（1σ）の2.5倍」で、揺れでは掛からない距離という原則で選び、
勝率を最大にする 3〜4倍 までは広げない（1回の損の率が大きくなり、ボードの「損切り12%以内」から外れる銘柄が増えるため）。

**利確の上限**: 20営業日のふだんの値幅（日々の値幅×√20）の1.5倍までに抑えた。平均R・勝率はほぼ同じ（+0.28R・53%）で、
表示する R倍が現実的になる（ボードの上位の計画上のR倍は 4〜5 → 2〜2.7）。

**型を足す**: 定番の形を4つ候補にして同じ方法で測り、2つを足した。

| 候補 | 20日後 全銘柄比 | 上回った割合 | 計画の平均R・勝率 | 採否 |
|---|---|---|---|---|
| 25日線の上抜け（前日は25日線の下、20日騰落マイナス） | −1.6% | 42% | +0.17R・48% | 見送り |
| 60日高値のブレイク（25日線から+10%未満） | −1.5% | 40% | −0.03R・38% | 見送り（リーダーと同じく今は負ける順張り） |
| **深押し**（60日+5%以上・25日線≧75日線で、5日 −7%以下） | **+0.9%** | 53% | +0.17R・61% | 採用 |
| **上向き転換**（25日線が5営業日前より上・株価＞25日線・25日線＜75日線・乖離+8%以内） | **+2.0%** | 59% | +0.71R・68% | 採用 |

**正直に書いておくこと**: この2つは、同じ期間のデータを見てから選んだ。選んだ期間の成績は良く見えやすい（多重比較）。
上向き転換は最初「当日プラス」を条件にしていたが、毎日出入りして件数が水増しされるので「25日線から+8%以内」に替えた。
深押しは −6% や値幅基準も試したが、最初に決めた −7% のままにした（閾値を成績で選ばない）。
判定は毎日その日までのデータで測り直すので、10月以降に新しく点灯した分で効き目が確かめられる。画面の「いま効いている型」にも
このことを書いている。

### 11.2 作ったもの

```
dashboard/thermo.py     STOP_MIN_VOL/STOP_MAX_VOL 2.5/3.5、TARGET_MAX_SIGMA 1.5（利確は target_by="cap"）
                        stock_metrics に slope25（25日線の5営業日の変化率）
                        stock_class に「深押し」「上向き転換」、SETUPS・BOARD_SETUPS に追加
                        action_board: 効いていない型だけに当てはまる銘柄は載せない。複数の型なら成績の良い型で。
                                      並びは 型の判定 → 台帳 → 型の平均R → R倍
                        market_stance: 今日のスタンス（下の表）
dashboard/thermo_run.py thermo.json と各区分の要約に stance、lists に deep / turn、判定の記録にも深押し・上向き転換
dashboard/commentary.py 温度計の節の先頭にスタンス。買いの型の判定に新しい型も含める。中立の帯の読み方を
                        「売られすぎを拾う」から「効いている型の候補を拾う」に（効いていない型を勧めない）
dashboard/build.py      carry_over: 同じ日の同じ区分を取り直したとき、取れなくなった区画（東証33業種・表・記事）は前回を引き継ぐ
                        --insurance: 保険の cron は、その日の区分が既にあれば何もしない（dashboard.yml が schedule のときに付ける）
                        寄り前: 09:00 以降に走ったら CNBC の prev（前日終値）を基準にする
docs/app.js             今日の結論カード（今日タブの2枚目・作戦タブの先頭）、作戦タブの並び替えとジャンプ、
                        株数をスタンスで調整（切り替え可）、深押し・上向き転換の一覧、買う前チェックに日足チャート
                        （終値・25日線・75日線・買う目安・損切り・利確の横線。bars.json はチェックを開いたときだけ読む）
```

**今日のスタンス**（`market_stance`。すべて公開の定数）

| 材料 | ＋1 | −1 |
|---|---|---|
| 地合い | 日経平均 ＞ 25日線 ＞ 75日線 | 日経平均 ＜ 25日線 ＜ 75日線 |
| 型の成績 | 全銘柄を上回っている買いの型がある | 無い |
| 温度帯の過去の成績 | この帯の20日後の中央値が全期間より +1pt 以上良い | 1pt 以上悪い |
| 過熱 | — | 温度が「過熱」 |

合計 2以上 **攻め**（株数 ×1）、0〜1 **選んで小さく**（×0.5）、−1以下 **守り**（×0.25）。
倍率は「1回の損の上限」に掛ける（負けやすい局面で連敗の傷を浅くする、資金管理の目安）。予測ではない。
9/25 大引の実データでは「選んで小さく」（地合い ±0: 日経 66,364 は25日線の上・75日線の下、型 +1: 深押し・上向き転換、
温度 44 中立 ±0: 中立の帯の20日後 +1.4% と全期間 +2.0% の差は1pt未満）。

### 11.3 採点（依頼の「勝率」と「使いやすさ」に効く重さで）

| 観点 | 配点 | 前 | 後 | 根拠 |
|---|---|---|---|---|
| 計画が勝ちやすい形か（損切り・利確） | 20 | 10 | 16 | 勝率 45→53%（押し目）、R倍は届く距離で。終値だけの再現・場中の高安とコストは含まない |
| 負けている型で入らない | 15 | 7 | 13 | 効いていない型はボードから外す。新しい2つの型は同じ期間で選んだ（−2） |
| 1画面目で「今日どうするか」が分かる | 20 | 8 | 17 | 結論カード（スタンス・根拠・買うなら3銘柄と株数・避ける・見張り） |
| 資金管理 | 10 | 7 | 9 | 地合いで株数を自動で減らす（切り替え可） |
| 取得データの品質 | 15 | 9 | 13 | 保険の cron による劣化・寄り前の基準値のずれを修正。出来高・高安（流動性・ATR）はまだ（−2） |
| 見やすさ（チャート・移動） | 10 | 6 | 9 | 買う前チェックに日足と計画の横線、作戦タブのジャンプ |
| 正直さ | 10 | 8 | 10 | 検証の表・選び方の偏りを画面と文書に書く |
| **合計** | **100** | **55** | **87** | |

### 11.4 限界と、次にやること

- 出来高・高値・安値を使っていない。CNBC の日足 API は応答に高安・出来高を含む可能性が高いが、この環境からは確かめられないので
  今回は触っていない。次に Actions の疎通診断で応答の形を確かめ、①売買代金（流動性）でボードを絞る ②値幅を ATR にする
  ③損切りの判定を場中の安値にする、の順で入れる。
- スタンスの境目（±1pt、2点以上で攻め）は運用で見直す。直したら tests も直す。
- 深押し・上向き転換の効き目は、10月以降に新しく点灯した分（判定の成績 `thermo_track.json`）で確かめる。
